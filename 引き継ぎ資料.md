# 📋 サークル管理Bot 引き継ぎ資料・運用マニュアル

この資料は、Discordサークル管理Botの**日常の運用方法**、**トラブル時の対処法**、および**システムの保守・引き継ぎ手順**をわかりやすくまとめたマニュアルです。

---

## 📌 目次
1. [Botの概要・できること](#1-botの概要できること)
2. [日常の使い方・操作ガイド](#2-日常の使い方操作ガイド)
3. [クラウド稼働の仕組み（Discloud）](#3-クラウド稼働の仕組みdiscloud)
4. [困ったときのトラブルシューティング](#4-困ったときのトラブルシューティング)
5. [設定の変更・新機能の追加手順](#5-設定の変更新機能の追加手順)
6. [⚠️ 運用の重要ルール（禁止事項）](#6-️-運用の重要ルール禁止事項)

---

## 1. Botの概要・できること

このBotは、サークルの「出欠確認」と「部室・施設の施錠/開放状況」をDiscord上で自動管理するシステムです。
**コマンド入力（`/`）は不要**で、管理用チャンネルに常設される**ボタンを押すだけ**ですべて操作できます。

```
┌─────────────────────────────────────────────────────────────┐
│                    Botの主要な2大機能                       │
├────────────────────────┬────────────────────────────────────┤
│ 📅 予定・出欠管理      │ Discordイベント連動 / ⭕❌🔺集計   │
│ 🚪 部室・施設状況管理  │ 🟢開放中 / 🔴施錠中 のリアルタイム共有 │
└────────────────────────┴────────────────────────────────────┘
```

---

## 2. 日常の使い方・操作ガイド

### A. 出欠管理の使い方
1. **Discord標準の「イベント機能」で本日の予定を作成**します。
2. 管理チャンネルにある管理パネルの **「📅 本日のイベントから出欠作成」** を選択すると、出欠パネルが自動送信されます。
3. 部員はパネルの **「⭕ 出席」「❌ 欠席」「🔺 遅刻・早退」** ボタンを押すだけで登録完了です（誰がどの状態かもリアルタイムで自動集計・更新されます）。
4. ※突発的な予定の場合は、**「✍️ 手動で出欠作成」** ボタンから自由な名前で出欠パネルを作成できます。

> [!NOTE]
> イベント連動の出欠パネルは、日付が変わると自動でデータベースおよびチャンネルからクリーンアップ（削除）されます。

---

### B. 部室・施設状況の使い方
1. 部室を開けた人は、管理パネルから該当する部屋を選んで **「🟢 開放」** に切り替えます。
2. 部室を出て施錠した人は、**「🔴 施錠」** に切り替えます。
3. 一覧パネルが自動で書き換わり、部員全員に現在の状況が共有されます。

---

## 3. クラウド稼働の仕組み（Discloud）

Botはクラウドホスティングサービス **「[Discloud](https://discloud.com)」** 上で24時間365日動いています。

* **作業PCの電源は切ってOK**: 部室や個人のPCをつけっぱなしにする必要はありません。
* **完全無料**: クレジットカード決済や有料契約は一切不要のプランで運用されています。
* **自動復帰機能**: 通信切断やエラーが起きても、クラウド側が自動で再起動（`AUTORESTART`）して復旧します。

---

## 4. 困ったときのトラブルシューティング

### Q1. Botがオフラインになっていて動かない
1. ブラウザで [Discloud Dashboard](https://discloud.com/dashboard) を開きます。
2. **部活のDiscordアカウント** でログインします。
3. Botのステータスを確認し、**「Restart（🔄）」** ボタンを押して再起動します。

### Q2. 管理パネルが消えてしまった
* Botを再起動（DiscloudでRestart）すると、管理チャンネルに管理パネルが**自動で再生成（自己修復）**されます。

### Q3. ボタンを押したのに「このインタラクションは失敗しました」と出る
* Botが一時的に再起動中か、通信が混雑している可能性があります。数秒待ってからもう一度押してください。

---

## 5. 設定の変更・新機能の追加手順

チャンネルの変更や新機能を追加した場合は、以下の手順でクラウドへ反映します。

```
┌─────────────────────────────────────────────────────────────┐
│                 更新・再デプロイの 3ステップ                │
├─────────────────────────────────────────────────────────────┤
│ 1. 修正 & テスト : PC上で .env や main.py を修正して動作確認│
│ 2. ZIP作成      : PowerShellで bot_deploy.zip を再生成      │
│ 3. アップロード  : DiscloudダッシュボードにZIPをドラッグ     │
└─────────────────────────────────────────────────────────────┘
```

#### 【ステップ1】設定やコードの修正
* チャンネルIDやトークンを変更する場合 → [`.env`](file:///e:/部活/ディスコードBot/.env) を編集
* 機能やメッセージを変更する場合 → [`main.py`](file:///e:/部活/ディスコードBot/main.py) を編集

#### 【ステップ2】ZIPファイルの作成
PowerShellを開き、プロジェクトフォルダで以下のコマンドをそのまま実行します：
```powershell
Compress-Archive -Path main.py, requirements.txt, .env, bot_database.db, discloud.config -DestinationPath bot_deploy.zip -Force
```

#### 【ステップ3】Discloudへアップロード
1. [Discloud Dashboard](https://discloud.com/dashboard) にログイン。
2. 稼働中のアプリに、新しくできた **`bot_deploy.zip`** をドラッグ＆ドロップしてアップロード。
3. ステータスが **「Online 🟢」** になれば完了です。

---

## 6. ⚠️ 運用の重要ルール（禁止事項）

> [!CAUTION]
> **1. クレジットカード・決済情報は絶対に登録しないこと**
> Discloudは完全無料枠のままで運用を継続できます。「有料プランへのアップグレード（Platinum等）」の案内ボタンは触らないでください。

> [!WARNING]
> **2. `.env` ファイルやBotトークンを外部に公開しないこと**
> `.env` にはBotのパスワードに相当する「トークン」が含まれています。LINEや一般のチャット、公開GitHub等に貼り付けないでください。

> [!IMPORTANT]
> **3. 引き継ぎ時に渡すもの**
> - 本プロジェクトフォルダ一式（このマニュアルを含む）
> - 部活のDiscordアカウント（Discloudログイン用）
