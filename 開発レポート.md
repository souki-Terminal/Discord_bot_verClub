# サークル管理Bot 開発レポート

インターンシップの開始、おめでとうございます！
これまでに開発した「サークル管理Bot」の仕様や設計の工夫点などを、後から見返したり、他のメンバーに引き継いだりしやすいようにレポートとしてまとめました。

---

## 1. プロジェクト概要
プログラミング未経験のメンバーでも運用・引き継ぎができることを前提とし、Discord上でのサークルの「出欠管理」および「部室の施錠・開放状況の管理」を自動化・可視化するための専用Botです。

## 2. 動作環境と使用技術
* **言語**: Python 3
* **主要ライブラリ**:
  * `discord.py` (Discord APIの操作)
  * `aiosqlite` (非同期SQLiteによるデータベース管理)
  * `python-dotenv` (環境変数の安全な読み込み)
* **データ保存先**: `bot_database.db` (SQLite) / `.env` (機密情報)

## 3. 実装された主な機能

### A. Bot管理パネル (自己修復機能付き)
* スラッシュコマンド（`/`）を一切使わず、指定した管理用チャンネルに常設される「ボタン」だけで全ての操作が完結するUIを採用。
* **自己修復**: Botが起動した際、管理チャンネルにパネルが見当たらなければ自動で再送信（自己修復）する仕組みを搭載。

### B. Discord標準イベント連動型の「出欠管理」機能
* Discord標準の「イベント機能」で作成された「本日」の予定を自動で読み込み、管理パネルのプルダウンから選択するだけで出欠パネルを生成。
* **開催日チェック**: 生成された出欠パネル（⭕❌🔺）は、設定された「開催日（YYYY-MM-DD）」の当日になるまで登録をブロックし、今日ではない過去や未来のパネルを押そうとした場合は自動でパネルごと削除される仕様。
* **手動作成への対応**: 突発的な予定用に「手動で出欠作成」する機能も備え、こちらは作成後即座に出欠登録が可能。
* **自動クリーンアップ機能**: 5秒ごとの更新ループにて、イベント連携のパネルのうち「今日以外」のものはデータベースおよびチャンネルから自動で削除され、常に綺麗な状態を維持。

### C. 動的な「部室・施設状況管理」機能
* 部室の追加・削除を管理パネルから簡単に行え、現在の「開放中🟢/施錠中🔴」をリアルタイムで共有。
* 追加された部屋は、自動的にプルダウン（セレクトメニュー）の選択肢として生成される。
* **重複防止（1つだけ表示）**: 部屋パネルを再設置する際、チャンネルに残っている過去のBotのパネルをすべて自動で削除してから新しいものを出すことで、パネルが無限に増殖するのを防止。

## 4. 本番稼働に向けた「堅牢性」の工夫点

VPS等のクラウドサーバーで24時間365日稼働させることを見据え、以下の工夫を施しています。

1. **Persistent View (永続化)**
   Botのプログラムを再起動・更新しても、すでにDiscord上に送信されている「出欠ボタン」や「部屋の切り替えメニュー」がエラーにならずに動き続けるように設定 (`timeout=None`)。
2. **非同期データベース処理 (aiosqlite)**
   複数のメンバーが同時に出欠ボタンを押しても、データベース（SQLite）がロックされてエラーにならないよう、全て非同期（`async/await`）で処理。
3. **API制限（レートリミット）の回避**
   ボタンが押された際、押した本人には「一時的メッセージ（Ephemeral）」で即座に応答を返しつつ、全員が見るメインのパネル表示は `tasks.loop` を使って「5秒に1回まとめて書き換える（バッチ更新）」設計を採用。これによりDiscordからのアクセス制限ペナルティを回避。

## 5. 今後の引き継ぎに向けて
* **コード内のコメント**: `main.py` には、初心者でも処理の流れが追えるように、クラスや関数の役割について詳細な日本語コメントを残しています。
* **環境変数の分離**: トークンなどの機密情報はコードに直書きせず `.env` に分離しているため、コード自体は安全に引き継ぎ（GitHub等へのアップロード）が可能です。
* **DBの自動生成**: `bot_database.db` が存在しない場合は起動時に自動生成されるため、新環境への移行時は `main.py` と `.env` さえあればすぐに本番稼働を開始できます。

---
以上が今回の開発レポートとなります。インターンシップでのご活躍を応援しております！
