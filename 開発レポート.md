# サークル管理Bot 開発レポート

インターンシップの開始、おめでとうございます！
これまでに開発した「サークル管理Bot」の仕様や設計の工夫点などを、後から見返したり、他のメンバーに引き継いだりしやすいようにレポートとしてまとめました。

---

## 1. プロジェクト概要
プログラミング未経験のメンバーでも運用・引き継ぎができることを前提とし、Discord上でのサークルの「出欠管理」および「部室の施錠・開放状況の管理」を自動化・可視化するための専用Botです。

## 2. 動作環境と使用技術
* **言語**: Python 3
* **主要ライブラリ**:
  * `discord.py` (Discord APIの操作)
  * `aiosqlite` (非同期SQLiteによるデータベース管理)
  * `python-dotenv` (環境変数の安全な読み込み)
* **データ保存先**: `bot_database.db` (SQLite) / `.env` (機密情報)

## 3. 実装された主な機能

### A. Bot管理パネル (自己修復機能付き)
* スラッシュコマンド（`/`）を一切使わず、指定した管理用チャンネルに常設される「ボタン」だけで全ての操作が完結するUIを採用。
* **自己修復**: Botが起動した際、管理チャンネルにパネルが見当たらなければ自動で再送信（自己修復）する仕組みを搭載。

### B. Discord標準イベント連動型の「出欠管理」機能
* Discord標準の「イベント機能」で作成された「本日」の予定を自動で読み込み、管理パネルのプルダウンから選択するだけで出欠パネルを生成。
* **開催日チェック**: 生成された出欠パネル（⭕❌🔺）は、設定された「開催日（YYYY-MM-DD）」の当日になるまで登録をブロックし、今日ではない過去や未来のパネルを押そうとした場合は自動でパネルごと削除される仕様。
* **手動作成への対応**: 突発的な予定用に「手動で出欠作成」する機能も備え、こちらは作成後即座に出欠登録が可能。
* **自動クリーンアップ機能**: 5秒ごとの更新ループにて、イベント連携のパネルのうち「今日以外」のものはデータベースおよびチャンネルから自動で削除され、常に綺麗な状態を維持。

### C. 動的な「部室・施設状況管理」機能
* 部室の追加・削除を管理パネルから簡単に行え、現在の「開放中🟢/施錠中🔴」をリアルタイムで共有。
* 追加された部屋は、自動的にプルダウン（セレクトメニュー）の選択肢として生成される。
* **重複防止（1つだけ表示）**: 部屋パネルを再設置する際、チャンネルに残っている過去のBotのパネルをすべて自動で削除してから新しいものを出すことで、パネルが無限に増殖するのを防止。

## 4. 本番稼働に向けた「堅牢性」の工夫点

VPS等のクラウドサーバーで24時間365日稼働させることを見据え、以下の工夫を施しています。

1. **Persistent View (永続化)**
   Botのプログラムを再起動・更新しても、すでにDiscord上に送信されている「出欠ボタン」や「部屋の切り替えメニュー」がエラーにならずに動き続けるように設定 (`timeout=None`)。
2. **非同期データベース処理 (aiosqlite)**
   複数のメンバーが同時に出欠ボタンを押しても、データベース（SQLite）がロックされてエラーにならないよう、全て非同期（`async/await`）で処理。
3. **API制限（レートリミット）の回避**
   ボタンが押された際、押した本人には「一時的メッセージ（Ephemeral）」で即座に応答を返しつつ、全員が見るメインのパネル表示は `tasks.loop` を使って「5秒に1回まとめて書き換える（バッチ更新）」設計を採用。これによりDiscordからのアクセス制限ペナルティを回避。

## 5. 今後の引き継ぎに向けて
* **コード内のコメント**: `main.py` には、初心者でも処理の流れが追えるように、クラスや関数の役割について詳細な日本語コメントを残しています。
* **環境変数の分離**: トークンなどの機密情報はコードに直書きせず `.env` に分離しているため、コード自体は安全に引き継ぎ（GitHub等へのアップロード）が可能です。
* **DBの自動生成**: `bot_database.db` が存在しない場合は起動時に自動生成されるため、新環境への移行時は `main.py` と `.env` さえあればすぐに本番稼働を開始できます。

## 6. 本番運用環境の選定とデプロイ方針

部活アカウントへの移行および24時間安定稼働に向け、運用環境の比較検討を行いました。

### A. 環境選定における制約条件
1. **大学・部室PCの常時起動回避**: 防犯・電気代・大学ルール・火災予防の観点から、ローカルPCの24時間通電は行わない。
2. **決済・アカウント制約**: クレジットカード決済不可、大学専用ドメイン（`.ac.jp`）なし（一般的なGmail/Discordアカウント）。
3. **データ永続化**: 再起動時に出欠や部室データ（SQLite）が消去されないこと。

### B. 採用環境：Discloud (無料プラン)
* **選定理由**: Discord Bot専用ホスティングサービス（公式サイト: https://discloud.com ）。Discordアカウントのみで登録可能（クレカ不要）。ファイルストレージが永続化されるため `bot_database.db` のデータが維持される。
* **構成**: `discloud.config`（RAM 100MB割り当て）をプロジェクトに同梱し、ZIP化してWebダッシュボードへアップロードするだけでデプロイ可能。

### C. 部室でのデプロイ実行手順マニュアル（次回作業用）

次回部室で作業する際は、以下の **3ステップ（所要時間約5分）** でそのまま完了できます。

```
┌─────────────────────────────────────────────────────────────┐
│               Discloud デプロイ 3ステップ 概要               │
├─────────────────────────────────────────────────────────────┤
│ 1. 事前確認 : .env のトークンが部活アカウントのものか確認    │
│ 2. ログイン : https://discloud.com に部活Discordでログイン │
│ 3. アップロード: bot_deploy.zip をダッシュボードにドラッグ  │
└─────────────────────────────────────────────────────────────┘
```

#### 【ステップ1】事前準備（トークンとZIPの確認）
1. [`.env`](file:///e:/部活/ディスコードBot/.env) を開き、部活のBotトークン・チャンネルIDが正しく設定されているか確認する。
2. もしコードや設定を変更した場合は、PowerShellで以下のコマンドを実行して最新の `bot_deploy.zip` を再生成する：
   ```powershell
   Compress-Archive -Path main.py, requirements.txt, .env, bot_database.db, discloud.config -DestinationPath bot_deploy.zip -Force
   ```
   *(※すでにフォルダ内に最新の `bot_deploy.zip` が作成済みのため、変更がなければそのまま使えます)*

#### 【ステップ2】Discloud へのログイン
1. ブラウザで [**https://discloud.com**](https://discloud.com)（または [https://discloud.com/dashboard](https://discloud.com/dashboard)）を開く。
2. 画面右上の **「Login」**（または「Dashboard」）をクリック。
3. **部活のDiscordアカウント** で認証・ログインする（クレジットカード等の登録は一切不要）。

#### 【ステップ3】ZIPのアップロードと起動確認
1. ダッシュボード画面上部の **「Upload」** または **「Add App」** ボタンをクリック。
2. プロジェクトフォルダにある **`bot_deploy.zip`** をドラッグ＆ドロップして選択。
3. **「Upload」** を実行すると、自動でライブラリのインストールとBotの起動が開始される。
4. ステータスが **「Online 🟢」** に変われば成功！
5. Discordの管理チャンネルを開き、管理パネルからボタンが正常に動作するか確認する。

> [!TIP]
> **デプロイ完了後の注意点**
> - Discloud上で24時間365日クラウド稼働するため、**作業したパソコンの電源は切って問題ありません**。
> - 今後の引き継ぎは「部活のDiscordアカウント情報」を後輩に渡すだけで完了します。



---

## 7. 自身のプログラミングに対する多角的レビュー（5つの視点）

本システム（`main.py`）の設計・実装について、5つの視点から客観的な評価・レビューを実施しました。

```
┌─────────────────────────────────────────────────────────────┐
│                    多角的コードレビュー 概要                 │
├────────────────────────┬────────────────────────────────────┤
│ 1. アーキテクチャ・UI/UX│ Persistent View と完全ボタンUI設計 │
│ 2. パフォーマンス・負荷 │ 5秒バッチ更新によるAPI制限回避     │
│ 3. データ整合性・信頼性│ 非同期SQLite & ON CONFLICT UPSERT │
│ 4. 保守性・引き継ぎ容易性│ 初心者向けコメントと単一ファイル   │
│ 5. セキュリティ・堅牢性│ 機密情報分離と例外ハンドリング     │
└────────────────────────┴────────────────────────────────────┘
```

### ① アーキテクチャ & UI/UX 設計の視点
* **評価点**:
  * コマンド入力を全廃し、Discordのボタン・セレクトメニュー・モーダルに特化した完全GUI設計。初心者やスマホユーザーでも迷わず直感的に操作可能。
  * `timeout=None` を用いた **Persistent View** を採用し、Botやサーバーが再起動しても過去の出欠ボタンがそのまま機能し続ける耐久性を確保。
  * 管理パネルの「自己修復機能」により、誤ってパネルが削除されても再起動時に自動復元される安心設計。
* **改善の余地**:
  * 部屋の数が増加して25件（Discordセレクトメニューの上限）を超えた場合のページネーション対応。

### ② パフォーマンス & APIレートリミット対策の視点
* **評価点**:
  * 出欠登録時にボタンを押したユーザーへは即座に応答（`defer(ephemeral=True)`）を返しつつ、全員が見るメッセージ本体の再描画は `tasks.loop(seconds=5.0)` による **バッチ更新** を採用。
  * Discord APIのメッセージ編集レートリミット（5秒間に数回程度）に抵触してBotが一時停止（HTTP 429）するリスクを構造的に防止。
  * メッセージ内容に変更がない場合は `msg.edit()` をスキップする差分チェック機構を実装し、無駄なAPI通信をカット。
* **改善の余地**:
  * イベント数が数十件に膨らんだ場合、1回のループ処理内で直列に `fetch_message` を呼ぶと時間がかかるため、将来的な並列フェッチ化（`asyncio.gather`）。

### ③ データ整合性 & 信頼性の視点
* **評価点**:
  * `aiosqlite` を用いた完全非同期DBアクセスにより、複数人が同時に出欠ボタンを押した際のブロッキング・応答遅延を防止。
  * `INSERT ... ON CONFLICT(message_id, user_id) DO UPDATE`（UPSERT構文）を用いており、「出席→やっぱり欠席」などの変更時にも重複行が発生しないクリーンなスキーマ設計。
  * 今日以外の古いイベントパネルや削除済みメッセージを自動検知してDB・チャンネルから削除するクリーンアップ機構により、ゴミデータが蓄積しない。
* **改善の余地**:
  * DBコネクションを毎回 `async with aiosqlite.connect()` で開閉しているため、高負荷時にはコネクションプール（または単一の長期接続）にすることでオーバーヘッドをさらに削減可能。

### ④ 保守性 & 引き継ぎ容易性の視点
* **評価点**:
  * 全てのクラス・主要関数に丁寧な日本語の解説コメントを記述。プログラミング未経験の後輩が読んでも「何のためにこのコードがあるか」が追従可能。
  * 外部設定（チャンネルID・トークン）を `.env` に完全分離し、コード本体を書き換えることなく設定変更が可能。
* **改善の余地**:
  * 現在は `main.py` の1ファイル（約530行）に全機能が集約されているため、引き継ぎやすさの観点では分かりやすい反面、機能拡張が進む場合は `cogs/` ディレクトリに機能別（出欠・部室・管理）に分割するモジュール化が望ましい。

### ⑤ セキュリティ & エラーハンドリングの視点
* **評価点**:
  * Discord Botトークンなどの機密情報を `.gitignore` 対象の `.env` で管理し、リポジトリ公開時の漏洩リスクを遮断。
  * **権限の二重チェック（`interaction_check`）**: 管理パネル（`AdminPanelView`）において、Discordのチャンネル権限設定だけでなく、コード側でも「管理者」「サーバー管理」「チャンネル管理」の各権限を検証。権限のない一般部員が誤ってボタンを押しても処理を自動ブロックする強固なセキュリティを実装。
  * メッセージ削除済み時（`discord.NotFound`）やDB操作時の重複エラー（`IntegrityError`）など、想定される例外を個別に捕捉してBot全体のクラッシュを防止。


---

## 8. まとめ

本Botは、**「誰でも直感的に使えるUI」「24時間連続稼働に耐える堅牢な非同期設計」「クレカ不要のクラウド運用対応」** を実現した、実用性の高いシステムとして完成しています。
今後はDiscloud等のクラウド環境を活用し、安全かつ持続可能な部活運営をサポートしていくことが期待されます。

