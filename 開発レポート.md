# サークル管理Bot 開発・運用レポート

本ドキュメントは、「サークル管理Bot」のシステム仕様、設計方針、クラウド運用環境の選定経緯、トラブルシューティング記録、および新機能追加ガイドをまとめた包括的開発レポートです。

---

## 1. プロジェクト概要
プログラミング未経験のメンバーでも運用・引き継ぎができることを前提とし、Discord上でのサークルの「出欠管理」および「部室・施設の施錠・開放状況の管理」を自動化・可視化するための専用Botです。

---

## 2. 動作環境と使用技術
* **言語**: Python 3
* **主要ライブラリ**:
  * `discord.py` (Discord APIの操作・UIコンポーネント)
  * `aiosqlite` (ローカルテスト用 非同期SQLite)
  * `libsql-client` (Turso クラウドSQLiteクライアント)
  * `aiohttp` (Render ポート開放用 Keep-Alive Webサーバー)
  * `python-dotenv` (環境変数の安全な読み込み)
* **データ保存先**:
  * ローカル環境: `bot_database.db` (SQLite)
  * クラウド本番環境: **Turso Cloud SQLite**
* **稼働インフラ**: **Render (Web Service)** ＋ **UptimeRobot (常時稼働監視)**

---

## 3. 実装された主な機能

### A. Bot管理パネル (自己修復機能付き)
* スラッシュコマンド（`/`）を一切使わず、指定した管理用チャンネルに常設される「ボタン」だけで全ての操作が完結するUIを採用。
* **自己修復**: Botが起動した際、管理チャンネルにパネルが見当たらなければ自動で再送信（自己修復）する仕組みを搭載。
* **権限チェック**: 管理者・サーバー管理・チャンネル管理権限を持つ部員のみが操作可能。

### B. Discord標準イベント連動型の「出欠管理」機能
* Discord標準の「イベント機能」で作成された「本日」の予定を自動で読み込み、管理パネルのプルダウンから選択するだけで出欠パネルを生成。
* **開催日チェック**: 生成された出欠パネル（⭕❌🔺）は、設定された「開催日（YYYY-MM-DD）」の当日になるまで登録をブロックし、今日ではない過去や未来のパネルを押そうとした場合は自動でパネルごと削除される仕様。
* **手動作成への対応**: 突発的な予定用に「手動で出欠作成」する機能も備え、こちらは作成後即座に出欠登録が可能。
* **自動クリーンアップ機能**: 5秒ごとの更新ループにて、イベント連携のパネルのうち「今日以外」のものはデータベースおよびチャンネルから自動で削除され、常に綺麗な状態を維持。

### C. 動的な「部室・施設状況管理」機能
* 部室の追加・削除を管理パネルから簡単に行え、現在の「開放中🟢/施錠中🔴」をリアルタイムで共有。
* 追加された部屋は、自動的にプルダウン（セレクトメニュー）の選択肢として生成される。
* **重複防止（1つだけ表示）**: 部屋パネルを再設置する際、チャンネルに残っている過去のBotのパネルをすべて自動で削除してから新しいものを出すことで、パネルが無限に増殖するのを防止。

---

## 4. 本番稼働に向けた「堅牢性」の工夫点

1. **Persistent View (UI永続化)**
   Botのプログラムを再起動・更新しても、すでにDiscord上に送信されている「出欠ボタン」や「部屋の切り替えメニュー」がエラーにならずに動き続けるよう `timeout=None` を設定。
2. **5秒バッチ更新によるAPI制限（レートリミット）の回避**
   ボタンが押された際、押した本人には「一時的メッセージ（Ephemeral）」で即座に応答を返しつつ、全員が見るメインのパネル表示は `tasks.loop(seconds=5.0)` でまとめて書き換える差分バッチ更新を採用。Discordからのアクセス制限ペナルティ（HTTP 429）を構造的に防止。
3. **ローカルSQLite & TursoクラウドDBの透過的ハイブリッド設計**
   `Database` 抽象化ラッパーを実装。環境変数にTursoの設定があれば自動でクラウドDBに接続し、ローカル開発時は自動で `bot_database.db` にフォールバックする安心設計。

---

## 5. クラウド運用インフラの選定とアーキテクチャ

部活運用における制約（**①クレカ決済不可、②大学PCの常時起動回避、③データ永続化、④24時間365日常時稼働**）をクリアするため、以下の3大サービスを組み合わせたシステムを採用しました。

```
┌────────────────────────────────────────────────────────────────────────┐
│                        本番クラウドアーキテクチャ                      │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│   ┌────────────────┐   HTTP Ping (5分間隔)    ┌─────────────────────┐  │
│   │  UptimeRobot   │ ───────────────────────> │  Render Web Service │  │
│   │ (スリープ防止) │                          │     (Bot 本体)      │  │
│   └────────────────┘                          └──────────┬──────────┘  │
│                                                          │             │
│                                           HTTPS Pipeline │ SQLクエリ   │
│                                                          v             │
│                                               ┌─────────────────────┐  │
│                                               │     Turso Cloud     │  │
│                                               │   (永続化 SQLite)   │  │
│                                               └─────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
```

### なぜこの構成を選定したのか？
* **Render (Web Service)**: GitHub連携でコミットするだけで自動デプロイ。無料枠（月750時間）で1つのBotを丸1ヶ月（最大744時間）完全に無停止で稼働可能。
* **UptimeRobot**: 無料枠の15分アイドルスリープを、5分おきの軽量HTTPアクセス（Keep-Alive）によって完全回避。
* **Turso**: 完全無料・クレカ不要で利用できるクラウドSQLite。Renderが再起動しても出欠や部室データが絶対に消去されない。

---

## 6. 実装における技術的トラブルシューティング記録

デプロイおよび稼働確認の過程で発生した問題と、その恒久対策の記録です。

### ① `libsql_client` のクライアント生成関数の不一致
* **事象**: `AttributeError: module 'libsql_client' has no attribute 'create_client_async'`
* **原因**: 公式クライアントライブラリの仕様において、非同期クライアントの標準ファクトリ関数は `libsql_client.create_client` であるため。
* **対策**: `libsql_client.create_client(url=..., auth_token=...)` に統一修正。

### ② WebSocketハンドシェイクエラー (400 Bad Request)
* **事象**: `WSServerHandshakeError: 400, message='Invalid response status', url='wss://...'`
* **原因**: `libsql://` や `wss://` プロトコルによるWebSocket接続がクラウドプロキシ環境等で失敗したため。
* **対策**: URLのプロトコルを正規表現で自動検知し、安全確実な **HTTPパイプライン（`https://`）形式に自動変換** する機構を実装。どんな形式で環境変数が渡されても正常に接続できるように堅牢化。

### ③ 定期更新ループの堅牢化
* **対策**: 5秒ごとのバッチ更新タスク（`update_attendance_panels` / `update_room_panels`）全体を包括的例外ハンドリングで保護し、ネットワーク瞬断時にもBotがクラッシュせず自動復旧する構造を確立。

---

## 7. システム設計の多角的レビュー（5つの視点）

```
┌─────────────────────────────────────────────────────────────┐
│                    多角的コードレビュー 概要                 │
├────────────────────────┬────────────────────────────────────┤
│ 1. アーキテクチャ・UI/UX│ Persistent View と完全ボタンUI設計 │
│ 2. パフォーマンス・負荷 │ 5秒バッチ更新によるAPI制限回避     │
│ 3. データ整合性・信頼性│ TursoクラウドDB & UPSERT構文       │
│ 4. 保守性・引き継ぎ容易性│ 初心者向けコメントとGitHub自動連携 │
│ 5. セキュリティ・堅牢性│ 機密情報完全分離と多重権限チェック │
└────────────────────────┴────────────────────────────────────┘
```

1. **アーキテクチャ & UI/UX**: コマンド全廃のボタンUI、管理パネルの自己修復機能により、スマホや初心者でも迷わず運用可能。
2. **パフォーマンス**: 差分更新と5秒バッチ処理により、Discord APIのレートリミットを回避。
3. **データ整合性**: `ON CONFLICT DO UPDATE`（UPSERT）とTursoクラウド永続化により、データ破損やリセットを根絶。
4. **保守性・引き継ぎ容易性**: 機密情報は環境変数に分離し、GitHubにプッシュするだけで自動更新される最新DevOpsパイプラインを構築。
5. **セキュリティ**: Discordのチャンネル権限に加え、コード側での管理者権限チェック（`interaction_check`）を実装。

---

## 8. 新機能追加・保守ガイド

今後、新機能（リマインダーやアンケートなど）を追加する際の手順です：

1. **ローカルで修正・テスト**: 手元のPCで `main.py` を修正し、`python main.py` で動作確認。
2. **ライブラリ追加時**: 新しい外部ライブラリを使用した場合は `requirements.txt` に追記。
3. **GitHubへプッシュ**:
   ```powershell
   git add .
   git commit -m "feat: 新機能の名前"
   git push origin main
   ```
4. **自動反映**: RenderがGitHubの更新を検知し、数分で自動的に最新版がクラウド上にデプロイされます。

---

## 9. まとめ

本Botは、**「誰でも直感的に使えるUI」「24時間365日完全無料・完全放置のクラウド運用」「データ永続化と堅牢なエラー耐性」** をすべて兼ね備えたシステムとして完成しました。
引き継ぎマニュアルと本レポートを活用することで、将来にわたり持続可能なサークル運営が実現されます。
